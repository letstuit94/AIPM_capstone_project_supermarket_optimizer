# Copy to .env.local for local development against a non-default backend
# port. On Vercel, set VITE_API_BASE_URL as a Project Environment
# Variable instead (Settings -> Environment Variables) to the deployed
# Render backend URL, e.g. https://nutriwise-backend.onrender.com

VITE_API_BASE_URL=http://localhost:8000
