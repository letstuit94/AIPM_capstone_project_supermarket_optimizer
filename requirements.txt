# ==========================
# Web API
# ==========================
fastapi==0.139.0
uvicorn==0.50.1
python-multipart==0.0.32

# ==========================
# Environment variables
# ==========================
python-dotenv==1.2.2

# ==========================
# Supabase
# ==========================
supabase==2.31.0
postgrest==2.31.0
storage3==2.31.0

# ==========================
# Google Gemini
# ==========================
google-genai==2.10.0

# ==========================
# HTTP requests
# ==========================
requests==2.34.2
httpx==0.28.1

# ==========================
# Data validation
# ==========================
pydantic==2.13.4
pydantic-settings==2.14.2

# ==========================
# Auth (E1) — verify Supabase user access tokens (JWT). The `[crypto]`
# extra pulls in `cryptography`, required for the ES256/RS256 asymmetric
# signing keys Supabase issues to signed-in users (see services/auth.py).
# ==========================
PyJWT[crypto]==2.10.1

# ==========================
# OpenFoodFacts nutrition lookups (Epic 2) are done via direct REST
# calls through `requests` (see backend/app/services/off_api.py), not
# the openfoodfacts SDK — no separate package needed.
# ==========================

# ==========================
# BLS 4.0 food-composition table (Epic 4). openpyxl parses the xlsx once
# to build backend/app/services/_bls_cache.json; only needed to (re)build
# that cache — the app runs from the cached JSON at runtime.
# ==========================
openpyxl==3.1.5